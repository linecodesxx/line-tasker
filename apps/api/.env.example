# Local development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/line_tasker
JWT_SECRET=change-me-in-local-dev
PORT=3001

# Production notes
# On Render, do not commit a real .env file.
# Set DATABASE_URL, JWT_SECRET, and optionally PORT in the Render dashboard.
# Render usually provides PORT automatically for web services.
